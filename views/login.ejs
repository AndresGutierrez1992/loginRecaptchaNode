<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login Seguro con Captcha</title>
  <!-- Bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  <style>
    body {
      background: linear-gradient(135deg, #6dd5fa, #2980b9);
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .login-container {
      background: #fff;
      border-radius: 12px;
      padding: 2rem;
      box-shadow: 0px 4px 20px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 400px;
    }
    .captcha-img svg {
      width: 100%;
    }
    .error {
      color: red;
      margin-bottom: 1rem;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h3 class="text-center mb-4">🔐 Iniciar Sesión</h3>

    <% if (error) { %>
      <div class="alert alert-danger text-center"><%= error %></div>
    <% } %>

    <form action="/login" method="POST">
      <div class="mb-3">
        <label class="form-label">Usuario</label>
        <input type="text" name="username" class="form-control" required />
      </div>

      <div class="mb-3">
        <label class="form-label">Contraseña</label>
        <input type="password" name="password" class="form-control" required />
      </div>

      <div class="mb-3">
        <label class="form-label">Captcha</label>
        <div class="captcha-img text-center mb-2">
          <% if (captchaSvg) { %>
            <%- captchaSvg %>
          <% } else { %>
            <img src="/captcha" alt="Captcha" class="img-fluid" />
          <% } %>
        </div>
        <div class="text-center mb-2">
          <button type="button" class="btn btn-sm btn-secondary" onclick="reloadCaptcha()">🔄 Recargar</button>
        </div>
        <input
          type="text"
          name="captcha"
          class="form-control"
          placeholder="Ingrese el texto del captcha"
          required
          autocomplete="off"
        />
      </div>

      <button type="submit" class="btn btn-primary w-100">Entrar</button>
    </form>
  </div>

  <script>
    function reloadCaptcha() {
      fetch('/captcha')
        .then(res => res.text())
        .then(svg => {
          document.querySelector('.captcha-img').innerHTML = svg;
        });
    }
  </script>
</body>
</html>
